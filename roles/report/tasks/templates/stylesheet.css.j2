/* Most of this CSS stuff is copypasta from https://www.w3schools.com */
/* To generate your own CSS stylesheet, see: https://www.w3schools.com/css/css_templates.asp*/

/* Note that we use a variable from the playbook to tune the navbar width ( {{ nav_width|default('200') }} )*/

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: Arial, Helvetica, sans-serif;
}

/* Style the side navigation */
.sidenav {
  height: 100%;
  width: 200px;
  position: fixed;
  z-index: 1;
  top: 0;
  left: 0;
  background-color: #336666;

  overflow-x: hidden; /* Disable horizontal scroll */
  padding-top: 10px; /* Place content 60px from the top */
  transition: 0.5s;
}


/* Side navigation links */
.sidenav a {
  color: white;
  padding: 8px;
  text-decoration: none;
  display: block;
  text-align: left;
  font-weight: normal;
  font-size: small;
}

/* Side navigation text */
.sidenav p {
  color: lightgray;
  padding: 5px;
  text-decoration: none;
  display: block;
  text-align: center;
  font-weight: bold;
}

/* Change color on hover */
.sidenav a:hover {
  background-color: #ddd;
  color: black;
}

/* Style the content */
.content {
  margin-left: {{ nav_width | default('200px') }};
  padding-left: 10px;
}

/* Used for any "command output" boxes */
.code {
  background: #f9f9f9 none repeat scroll 0 0;
  border: 1px solid #aaa;
  display: table;
  font-size: 100%;
  font-family: monospace;
  margin-bottom: 1em;
  padding: 10px;
  width: 95%;
}

/* Enable smart text-wrapping for any 'pre' tags (command output) */
pre { white-space: pre-wrap; }

/* Misc. formatting for tables, headers, etc */
/*table { border:1px solid grey; }*/
table {
            border-collapse: collapse;
            width: 80%;
        }
th {
  padding: 2px;
  border: 1px solid grey;
  background-color: #336666;
  opacity: 100%;
  font-family: Arial, Helvetica, sans-serif;
  color: white;
}
td { padding: 2px; border: 1px solid grey;}
/*
tr:nth-child(even) {
            background-color: #C0C0C0;
        }
tr:nth-child(odd) {
            background-color: #F5F5F5;
        }*/
h1 { color: black; font-family: Tahoma, Verdana, sans-serif;font-weight: normal;}
h2 {color: black; font-family: Tahoma, Verdana, sans-serif;font-weight: normal; }
h3 {color: black; font-family: Tahoma, Verdana, sans-serif;font-weight: normal;}


#check_list_table.css

table.check_list_table {border-collapse:collapse;}
td.a {text-align: center;}