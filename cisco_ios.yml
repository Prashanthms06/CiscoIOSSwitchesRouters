  - hosts:
      - all
    gather_facts: no
    roles:
      - role: common

      - role: switch_port_config
        when: operation_type|upper=="SWITCH_PORT_CONFIGURATION"

      - role: vlan
        when: operation_type|upper=="VLAN_CONFIGURATION"